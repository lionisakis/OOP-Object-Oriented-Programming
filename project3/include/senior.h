#pragma once
#include <string>

#include "student.h"

class Senior:public Student {
    public:
        Senior(const std::string, const unsigned int ,const unsigned int,const unsigned int);
        
};